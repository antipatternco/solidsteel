<p id="notice"><%= notice %></p>

<h1>Listing Mixes</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @mixes.each do |mix| %>
      <tr>
        <td><%= mix.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

